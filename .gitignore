node_modules
.next
out
build
.DS_Store
*.pem
next-env.d.ts
tsconfig.tsbuildinfo
.vercel

# Secrets. The negation must come after the wildcard, or a later `.env*`
# rule (vercel link appends one) silently re-ignores the example file.
.env*
!.env.example
